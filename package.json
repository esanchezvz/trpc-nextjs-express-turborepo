{
  "name": "repo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "api:dev": "dotenv -e .env -e .env.development -- pnpm -F api dev",
    "web:dev": "dotenv -e .env -e .env.development -- pnpm -F web dev",
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  },
  "prettier": "@repo/prettier-config",
  "devDependencies": {
    "@types/node": "^20.11.4",
    "dotenv-cli": "^7.3.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.2",
    "typescript": "^5.3.3"
  }
}
